import 'package:flutter/material.dart';

class elquerane extends StatelessWidget {
  const elquerane({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(
          title: const Text(' بسم الله الرحمن الرحيم'),
        ),
      ),
    );
  }
}
